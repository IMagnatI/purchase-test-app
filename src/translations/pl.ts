import buttons from '@/translations/pl/buttons'
import general from '@/translations/pl/general'
import notifications from '@/translations/pl/notifications'

export default {
  ...buttons,
  ...general,
  ...notifications,
}
